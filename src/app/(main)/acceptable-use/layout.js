export const metadata = {
  title: 'Acceptable Use Policy',
  description: 'Truck Command acceptable use policy. Guidelines for using our trucking software platform.',
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: 'https://truckcommand.com/acceptable-use',
  },
};

export default function AcceptableUseLayout({ children }) {
  return children;
}
