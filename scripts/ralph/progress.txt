# Truck Command - Ralph Progress Log
# ===================================
# This file tracks learnings and patterns discovered during autonomous development.
# Ralph reads this at the start of each iteration to avoid repeating mistakes.

## Current Sprint: QuickBooks Integration Completion
## Started: 2026-01-09
## Objective: Enable the QuickBooks UI (backend is built, UI shows "Coming Soon")

## Codebase Patterns (IMPORTANT - Read First)
-------------------------------------------

### Database & Supabase
- All tables have `user_id` column for multi-tenant data isolation
- Use `formatError()` from `src/lib/supabaseClient.js` for consistent error messages
- RLS policies are enabled - queries automatically filter by authenticated user
- Always include `.eq('user_id', userId)` in manual queries
- Use `.select().single()` when expecting one result after insert/update

### Service Layer (`src/lib/services/`)
- Services export object with async methods
- Pattern: `{ getAll, getById, create, update, delete }`
- Always wrap Supabase calls in try/catch
- Return data directly, throw errors for handling by components

### React Components
- Use 'use client' directive for client components
- Get user from `useAuth()` hook: `const { user } = useAuth()`
- Standard state pattern: `[loading, setLoading]`, `[data, setData]`, `[error, setError]`
- Fetch data in `useEffect` when user is available
- Always show loading spinner: `<div className="loading loading-spinner loading-lg"></div>`
- Always show error state: `<div className="alert alert-error">{error}</div>`

### Styling (DaisyUI + Tailwind)
- Primary button: `btn btn-primary`
- Secondary button: `btn btn-secondary`
- Card container: `card bg-base-100 shadow-xl`
- Card body: `card-body`
- Form input: `input input-bordered w-full`
- Select dropdown: `select select-bordered w-full`
- Table: `table table-zebra w-full`
- Alert success: `alert alert-success`
- Alert error: `alert alert-error`
- Loading: `loading loading-spinner loading-lg`

### Git Commit Convention
- Prefix: `[Ralph] QB-XXX: Description`
- Example: `[Ralph] QB-001: Enable QuickBooks connection UI`
- Keep commits atomic - one feature per commit

## QuickBooks Integration Architecture (CRITICAL)
-------------------------------------------------

### Existing Services - DO NOT RECREATE, USE THESE:
- `src/lib/services/quickbooks/quickbooksConnectionService.js`
  - getAuthorizationUrl(userId, redirectUri, options)
  - handleOAuthCallback(code, stateParam, realmId, redirectUri)
  - getConnection(userId)
  - refreshTokens(connectionId)
  - disconnectConnection(userId)
  - verifyConnection(connectionId)
  - getConnectionStatus(userId)

- `src/lib/services/quickbooks/quickbooksSyncService.js`
  - syncExpense(connectionId, expense)
  - syncInvoice(connectionId, invoice)
  - bulkSyncExpenses(connectionId, userId, options)
  - bulkSyncInvoices(connectionId, userId, options)
  - getSyncStatus(userId)
  - getSyncHistory(connectionId, limit)
  - getSyncRecord(connectionId, entityType, localEntityId)
  - retryFailedSyncs(connectionId)

- `src/lib/services/quickbooks/quickbooksMappingService.js`
  - getMappings(connectionId)
  - getMappingForCategory(connectionId, category)
  - createMapping(connectionId, mapping)
  - updateMapping(mappingId, updates)
  - deleteMapping(mappingId)

### Existing API Routes - USE THESE:
- GET/POST `/api/quickbooks/connect` - Initiates OAuth, returns authUrl
- GET `/api/quickbooks/callback` - OAuth callback handler
- POST `/api/quickbooks/disconnect` - Disconnects QB
- POST `/api/quickbooks/sync` - Bulk sync endpoint
- GET `/api/quickbooks/status` - Connection and sync status
- GET `/api/quickbooks/accounts` - List QB chart of accounts
- GET/POST/DELETE `/api/quickbooks/mappings` - Category mapping CRUD

### Existing UI Components - INTEGRATE THESE:
- `QuickBooksSyncStatus.js` - Shows sync stats
- `QuickBooksBulkSyncModal.js` - Bulk sync with progress
- `QuickBooksCategoryMappingModal.js` - Category to QB account mapping

### QuickBooks Database Tables:
- `quickbooks_connections` - Connection record (tokens, company_name, auto_sync flags)
- `quickbooks_category_mappings` - TC category -> QB account ID mappings
- `quickbooks_sync_records` - Per-entity sync status (synced, pending, failed)
- `quickbooks_sync_history` - Bulk operation history with stats

## Key Files Reference
----------------------
- `src/lib/supabaseClient.js` - Database client, formatError utility
- `src/lib/protectedRoute.js` - Auth protection HOC
- `src/context/AuthContext.js` - Authentication context
- `src/context/SubscriptionContext.js` - Subscription state
- `CLAUDE.md` - Full project documentation

## UI Design Patterns for QuickBooks
------------------------------------
- QuickBooks brand color: `#2CA01C` (green) - use for QB-specific accents
- Use Lucide React icons: RefreshCw, Check, X, AlertCircle, Settings, Clock, Link
- Connection status badges:
  - Connected: `badge badge-success` with green dot
  - Disconnected: `badge badge-warning`
  - Error: `badge badge-error`
- Sync status indicators:
  - Synced: Green check icon
  - Pending: Yellow clock icon
  - Failed: Red X icon
  - Not synced: Gray dash

## Common Issues & Solutions
----------------------------

### Issue: "user is undefined"
Solution: Ensure component uses `useAuth()` and checks `if (user)` before data fetch

### Issue: QuickBooks API returns 401
Solution: Token may be expired. Call refreshTokens() first, retry the operation.

### Issue: Sync fails with "No mapping for category"
Solution: User needs to map TC categories to QB accounts first. Show mapping UI.

### Issue: Component not updating after sync
Solution: Call onSyncComplete callback, or refetch status after sync completes.

## Session Log
--------------
# Entries added automatically by Ralph during iterations

### [2026-01-09] Sprint Initialization
- Created prd.json with 8 stories for QuickBooks completion
- Backend services are complete - focus is on enabling UI
- Critical path: QB-001 (connection UI) -> QB-002 (sync button) -> QB-003 (mapping UI)
- Main file to update: src/components/expenses/QuickBooksIntegration.js
- Current state: Shows "Coming Soon" - needs to show real connection/sync UI

